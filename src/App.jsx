import React,{useEffect,useRef,useState} from 'react';
import {collection,deleteDoc,doc,getDoc,getDocs,limit,onSnapshot,orderBy,query,runTransaction,serverTimestamp,setDoc,updateDoc,where} from 'firebase/firestore';
import {onAuthStateChanged,signInWithEmailAndPassword,signOut} from 'firebase/auth';
import {db,auth} from './firebase'; import {Html5Qrcode} from 'html5-qrcode'; import QRCode from 'qrcode'; import {jsPDF} from 'jspdf';
const EVENT={id:'ramona-claudio',eventName:'Uitnodiging Ramona en Claudio',names:'Ramona & Claudio',subtitle:'43 & 45 Celebration',date:'Vrijdag 18 september 2026',time:'Inloop vanaf 19.00 u',location:'Lalarookh',dresscode:'All Black',ticketPrefix:'RC',maxTickets:100};
export default function App(){const[user,setUser]=useState(undefined),[role,setRole]=useState(null);useEffect(()=>onAuthStateChanged(auth,async u=>{setUser(u||null);setRole(null);if(u){const s=await getDoc(doc(db,'rc_users',u.uid));setRole(s.exists()?s.data().role:null)}}),[]);if(user===undefined)return <Splash/>;if(!user)return <Login/>;if(!role)return <Center><h2>Geen toegang</h2><p>Maak eerst je rol aan in <code>rc_users</code>.</p><button className="btn" onClick={()=>signOut(auth)}>Uitloggen</button></Center>;return role==='scanner'||location.pathname.toLowerCase().startsWith('/scanner')?<Scanner user={user}/>:<Admin/>}
function Login(){const[e,setE]=useState(''),[p,setP]=useState(''),[err,setErr]=useState('');async function go(x){x.preventDefault();try{setErr('');await signInWithEmailAndPassword(auth,e.trim(),p)}catch{setErr('Inloggen is niet gelukt.')}}return <div className="login"><form className="card" onSubmit={go}><small>RAMONA & CLAUDIO</small><h1>Event Ticketing</h1><p>Beheer en poortscanner</p><label>E-mailadres</label><input type="email" value={e} onChange={x=>setE(x.target.value)} required/><label>Wachtwoord</label><input type="password" value={p} onChange={x=>setP(x.target.value)} required/>{err&&<div className="alert">{err}</div>}<button className="btn full">Inloggen</button></form></div>}
function Admin(){const[tickets,setTickets]=useState([]),[settings,setSettings]=useState(EVENT),[count,setCount]=useState(1),[name,setName]=useState(''),[msg,setMsg]=useState('');useEffect(()=>{const a=onSnapshot(query(collection(db,'rc_event_tickets'),orderBy('ticketNumber')),s=>setTickets(s.docs.map(d=>({id:d.id,...d.data()}))));const b=onSnapshot(doc(db,'rc_event_settings',EVENT.id),s=>{if(s.exists())setSettings(v=>({...v,...s.data()}))});return()=>{a();b()}},[]);const used=tickets.filter(t=>t.status==='used').length,blocked=tickets.filter(t=>t.blocked).length,unused=tickets.filter(t=>t.status!=='used'&&!t.blocked).length;
async function generate(){const qty=Math.max(1,Math.min(Number(count)||1,50)),max=Number(settings.maxTickets||100);if(tickets.length+qty>max){setMsg(`Maximaal ${max} tickets.`);return}const seen=new Set(tickets.map(t=>t.ticketNumber));let made=0,n=1;while(made<qty){const num=`${settings.ticketPrefix||'RC'}-${String(n++).padStart(4,'0')}`;if(seen.has(num))continue;await setDoc(doc(db,'rc_event_tickets',num),{ticketNumber:num,token:makeToken(),status:'unused',blocked:false,scannedAt:null,scannedBy:null,createdAt:serverTimestamp(),guestName:qty===1?name.trim():'',eventId:EVENT.id,ticketType:'standard'});seen.add(num);made++}setName('');setMsg(`${qty} ticket${qty===1?'':'s'} gegenereerd.`)}
return <div className="shell"><header><div><small>UITNODIGING</small><h1>{settings.names||EVENT.names}</h1></div><button className="ghost" onClick={()=>signOut(auth)}>Uitloggen</button></header><main><section className="hero"><div><b>43 & 45 CELEBRATION</b><h2>{settings.eventName||EVENT.eventName}</h2><p>{settings.date||EVENT.date} · {settings.time||EVENT.time}</p><p>{settings.location||EVENT.location} · Dresscode: {settings.dresscode||EVENT.dresscode}</p></div><span>ONE-TIME ENTRY</span></section><section className="stats"><Stat n={tickets.length} t="Totaal"/><Stat n={unused} t="Nog niet gescand"/><Stat n={used} t="Binnen"/><Stat n={blocked} t="Geblokkeerd"/></section><section className="panel"><h3>Tickets genereren</h3><div className="formrow"><div><label>Aantal</label><input type="number" min="1" max="50" value={count} onChange={e=>setCount(e.target.value)}/></div><div><label>Naam (optioneel bij 1 ticket)</label><input value={name} onChange={e=>setName(e.target.value)} placeholder="Bijv. Familie Jansen"/></div><button className="btn" onClick={generate}>Genereer</button><button className="btn soft" onClick={()=>batchPdf(tickets.filter(t=>!t.blocked),settings)}>Alle PDF's</button></div>{msg&&<div className="alert">{msg}</div>}</section><section className="panel"><div className="titleline"><div><h3>Tickets</h3><p>Limiet: {settings.maxTickets||100}</p></div><a className="btn" href="/scanner">Open scanner</a></div><div className="table">{tickets.map(t=><div className="row" key={t.id}><strong>{t.ticketNumber}</strong><Status t={t}/><span>{t.guestName||'—'}</span><div className="actions"><button onClick={()=>ticketPdf(t,settings)}>PDF</button><button onClick={()=>updateDoc(doc(db,'rc_event_tickets',t.id),{blocked:!t.blocked})}>{t.blocked?'Deblokkeer':'Blokkeer'}</button><button className="danger" onClick={async()=>{if(confirm(`${t.ticketNumber} verwijderen?`))await deleteDoc(doc(db,'rc_event_tickets',t.id))}}>Verwijder</button></div></div>)}</div></section></main></div>}
function Scanner({user}){const[result,setResult]=useState(null),[manual,setManual]=useState(''),[active,setActive]=useState(false),ref=useRef(null);useEffect(()=>()=>stop(),[]);async function start(){setResult(null);try{const s=new Html5Qrcode('qr-reader');ref.current=s;setActive(true);await s.start({facingMode:'environment'},{fps:10,qrbox:{width:260,height:260}},async txt=>{await stop();await process(txt)},()=>{})}catch{setActive(false);setResult({type:'bad',title:'Camera niet beschikbaar',msg:'Controleer camera-toestemming.'})}}async function stop(){if(ref.current){try{if(ref.current.isScanning)await ref.current.stop();await ref.current.clear()}catch{}ref.current=null}setActive(false)}async function process(raw){const p=parse(raw);if(!p){setResult({type:'bad',title:'ONGELDIGE QR',msg:'Hoort niet bij dit event.'});return}const s=await getDocs(query(collection(db,'rc_event_tickets'),where('token','==',p.token),limit(1)));if(s.empty){setResult({type:'bad',title:'ONGELDIG',msg:'Ticket niet gevonden.'});return}await check(s.docs[0].ref)}async function check(r){try{const out=await runTransaction(db,async tx=>{const s=await tx.get(r);if(!s.exists())return{c:'bad'};const d=s.data();if(d.blocked)return{c:'blocked',d};if(d.status==='used')return{c:'used',d};tx.update(r,{status:'used',scannedAt:serverTimestamp(),scannedBy:user.uid});tx.set(doc(collection(db,'rc_event_scans')),{ticketNumber:d.ticketNumber,ticketId:r.id,scannerId:user.uid,scannedAt:serverTimestamp(),result:'accepted'});return{c:'ok',d}});setResult(out.c==='ok'?{type:'ok',title:'TOEGANG GOEDGEKEURD',msg:out.d.ticketNumber}:out.c==='used'?{type:'warn',title:'REEDS GEBRUIKT',msg:out.d.ticketNumber}:out.c==='blocked'?{type:'bad',title:'TICKET GEBLOKKEERD',msg:out.d.ticketNumber}:{type:'bad',title:'ONGELDIG',msg:'Niet gevonden'})}catch{setResult({type:'bad',title:'SCAN MISLUKT',msg:'Controleer internet en regels.'})}}async function manualGo(){const v=manual.trim().toUpperCase();if(v)await check(doc(db,'rc_event_tickets',v));setManual('')}
return <div className="scanpage"><header><div><small>RAMONA & CLAUDIO</small><h1>Gate Scanner</h1></div><button className="ghost dark" onClick={()=>signOut(auth)}>Uitloggen</button></header><main className="scanmain">{result?<div className={`result ${result.type}`}><div>{result.type==='ok'?'✓':result.type==='warn'?'!':'×'}</div><h2>{result.title}</h2><p>{result.msg}</p><button className="btn light" onClick={()=>setResult(null)}>Volgende ticket</button></div>:<><section className="scanner"><div id="qr-reader"></div>{!active&&<div className="placeholder"><h2>Scan ticket</h2><p>Open de camera en richt op de QR-code.</p><button className="btn" onClick={start}>Camera openen</button></div>}</section><section className="manual"><h3>Handmatige controle</h3><div><input value={manual} onChange={e=>setManual(e.target.value)} placeholder="RC-0001"/><button className="btn" onClick={manualGo}>Controleer</button></div></section></>}</main></div>}
function Stat({n,t}){return <div className="stat"><strong>{n}</strong><span>{t}</span></div>}function Status({t}){return <span className={`status ${t.blocked?'blocked':t.status==='used'?'used':'unused'}`}>{t.blocked?'Geblokkeerd':t.status==='used'?'Binnen':'Ongebruikt'}</span>}function Splash(){return <div className="center">Laden...</div>}function Center({children}){return <div className="center"><div className="panel">{children}</div></div>}function makeToken(){return crypto.randomUUID().replaceAll('-','')+crypto.randomUUID().replaceAll('-','')}function payload(t){return `RC-EVENT|${t.ticketNumber}|${t.token}`}function parse(x){const p=String(x).split('|');return p.length===3&&p[0]==='RC-EVENT'?{ticketNumber:p[1],token:p[2]}:null}
async function ticketPdf(t,s){const pdf=new jsPDF({orientation:'landscape',unit:'mm',format:[210,99]});await draw(pdf,t,s);pdf.save(`${t.ticketNumber}.pdf`)}async function batchPdf(ts,s){if(!ts.length)return;const pdf=new jsPDF({orientation:'landscape',unit:'mm',format:[210,99]});for(let i=0;i<ts.length;i++){if(i)pdf.addPage([210,99],'landscape');await draw(pdf,ts[i],s)}pdf.save('Ramona-Claudio-Tickets.pdf')}async function draw(pdf,t,s){const blush=[248,231,228],gold=[181,136,58],dark=[24,20,19],rose=[171,105,99];pdf.setFillColor(...blush);pdf.rect(0,0,210,99,'F');pdf.setFillColor(...dark);pdf.triangle(0,0,55,0,0,32,'F');pdf.triangle(0,99,50,99,0,71,'F');pdf.setDrawColor(...gold);pdf.rect(4,4,202,91);pdf.setLineDashPattern([2,2],0);pdf.line(158,5,158,94);pdf.setLineDashPattern([],0);pdf.setTextColor(...gold);pdf.setFont('helvetica','bold');pdf.setFontSize(12);pdf.text('UITNODIGING',81,16,{align:'center'});pdf.setTextColor(...dark);pdf.setFont('times','bold');pdf.setFontSize(25);pdf.text(s.names||EVENT.names,81,31,{align:'center'});pdf.setTextColor(...rose);pdf.setFont('helvetica','bold');pdf.setFontSize(9);pdf.text('43 & 45 CELEBRATION',81,39,{align:'center'});pdf.setTextColor(...dark);pdf.setFont('helvetica','normal');pdf.setFontSize(8.5);pdf.text(s.date||EVENT.date,81,52,{align:'center'});pdf.text(s.time||EVENT.time,81,59,{align:'center'});pdf.text(`Locatie: ${s.location||EVENT.location}`,81,66,{align:'center'});pdf.text(`Dresscode: ${s.dresscode||EVENT.dresscode}`,81,73,{align:'center'});if(t.guestName){pdf.setFont('helvetica','bold');pdf.text(t.guestName,81,80,{align:'center'})}pdf.setFillColor(...dark);pdf.roundedRect(52,84,58,9,2,2,'F');pdf.setTextColor(...gold);pdf.setFont('helvetica','bold');pdf.setFontSize(9);pdf.text('STANDARD TICKET',81,90,{align:'center'});const q=await QRCode.toDataURL(payload(t),{errorCorrectionLevel:'H',margin:2,width:500});pdf.setTextColor(...rose);pdf.setFontSize(8);pdf.text('SCAN FOR ENTRY',183.5,14,{align:'center'});pdf.addImage(q,'PNG',165,19,37,37);pdf.text('ONE-TIME ENTRY',183.5,62,{align:'center'});pdf.setFillColor(...dark);pdf.roundedRect(165,70,37,17,2,2,'F');pdf.setTextColor(...gold);pdf.setFontSize(7);pdf.text('TICKET NO.',183.5,76,{align:'center'});pdf.setFontSize(13);pdf.text(t.ticketNumber,183.5,83,{align:'center'})}
